# Gustavo Vinicius

## Commands
```bash
sudo php -S localhost:8080/test

docker compose up -d --build ubuntu
docker exec -it -u 0 ubuntu bash
service php8.1-fpm start
service nginx start
cd /var/www/html/api && composer install

```
